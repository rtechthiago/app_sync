on:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - node-version: "16"
    - bucket-name: "portal"
    - bucket-key: "portal"
